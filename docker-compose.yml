version: '3.8'

services:
  app:
    build: .
    ports:
      - "3001:3001"
    environment:
      - DATABASE_URL=file:./dev.db
      - REDIS_URL=redis://redis:6379
      - JWT_SECRET=change_me_in_prod
    depends_on:
      - redis

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
